# Minimal dependency set for the deployed serving image (Dockerfile).
# The full requirements.txt (torch, transformers, mlflow, evidently, ...)
# is for development / the training & tracking pipeline; none of that
# needs to ship in the production container, which only loads a
# pre-exported ONNX model and serves HTTP requests.
fastapi==0.141.1
uvicorn[standard]==0.46.0
pydantic==2.13.3
onnxruntime==1.25.0
numpy==2.4.4
